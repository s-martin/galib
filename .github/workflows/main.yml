name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: mkdir build && cd build
    - name: Run a one-line script
      run: cmake ../
    - name: Run a one-line script
      run: make
